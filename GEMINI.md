# プロジェクト概要

このプロジェクトは、Unity Editor バージョン `2021.3.45f1` で開発された Unity プロジェクトです。

主な目的は、インタラクティブな物語またはゲームを構築することであると推測されます。これは、`DialogueSystemConfigurationAsset.asset` の存在から、対話システムが重要な要素であることを示唆しています。

## 主要な技術と機能

このプロジェクトでは、以下の主要な Unity パッケージと機能が活用されています。

*   **Universal Render Pipeline (URP):** スケーラブルでカスタマイズ可能なグラフィックスを実現するために使用されます。これにより、様々なプラットフォームで最適化されたビジュアルを提供できます。
*   **Addressables:** アセットの効率的な管理と配信のために利用されます。これにより、アセットのロード時間を最適化し、動的なコンテンツ更新を容易にします。
*   **Input System:** 最新の入力処理フレームワークで、柔軟かつ拡張性の高いユーザー入力ハンドリングを提供します。
*   **TextMeshPro:** 高度なテキストレンダリング機能を提供し、高品質なUIテキストやゲーム内テキストを実現します。
*   **Visual Scripting:** (可能性として) ゲームロジックやエディタ拡張を視覚的に構築するために使用されている可能性があります。

# プロジェクト構造と仕組み

このプロジェクトは、標準的な Unity プロジェクトのディレクトリ構造に従っています。

*   **`Assets/`**: プロジェクトのすべてのゲームアセット（スクリプト、シーン、プレハブ、テクスチャ、オーディオなど）が格納されます。
    *   `_MAIN/`: メインのゲームコンテンツやコアロジックが配置されているようです。
        *   `MainAD.asmdef`: アセンブリ定義ファイル。C#スクリプトのコンパイルを高速化し、コードの依存関係を管理するために使用されます。
        *   `Configurations/`: ゲームの設定ファイル（例: `DialogueSystemConfigurationAsset.asset`）が格納されています。
    *   `_TESTING/`: テスト関連のアセットやシーンが格納されている可能性があります。
    *   `AddressableAssetsData/`: Addressables システムによって生成されたデータが格納されます。
    *   `Settings/`: URPレンダラー設定など、プロジェクト固有の設定ファイルが含まれます。
*   **`Library/`**: Unity が生成する一時ファイル、キャッシュ、ビルドデータなどが格納されます。このディレクトリはバージョン管理システム（Gitなど）の対象外とすべきです。
*   **`Packages/`**: プロジェクトで使用される Unity パッケージ（`manifest.json` で定義）が格納されます。
    *   `manifest.json`: プロジェクトの依存関係にある Unity パッケージとそのバージョンを定義します。
*   **`ProjectSettings/`**: Unity Editor の設定ファイルが格納されます（例: `ProjectVersion.txt`、`AudioManager.asset`、`GraphicsSettings.asset` など）。
    *   `ProjectVersion.txt`: プロジェクトが作成または最後に開かれた Unity Editor のバージョン情報が含まれます。
*   **`.vscode/`**: VS Code の設定ファイル（拡張機能の推奨、デバッグ設定など）が格納されます。
*   **`.git/`**: Git リポジトリのメタデータが格納されます。
*   **`.gitignore` / `.gitattributes`**: バージョン管理の対象外とするファイルや、Git の動作をカスタマイズするための設定ファイルです。

# ビルドと実行

このプロジェクトは主に Unity Editor 内で開発および管理されます。

プロジェクトを開くには：
1.  Unity Editor `2021.3.45f1` がインストールされていることを確認してください。
2.  Unity Hub を開き、このプロジェクトを追加して開きます。

テストを実行するには：
```bash
make test
```
このコマンドは、Unity Editor をバッチモードで起動し、指定されたテストプラットフォーム（EditMode）でテストを実行し、結果を `./TestResults/result.xml` に出力します。

# 開発規約

*   **プロジェクト構造:** `Assets`、`Packages`、`ProjectSettings` など、標準的な Unity プロジェクトのフォルダ構造に従っています。
*   **スクリプト言語:** C# が主要なスクリプト言語です。
*   **コード構成:** `Assets` フォルダ内では、アセンブリ定義ファイル（`.asmdef`）を使用してコードを論理的なモジュールに分割しています。これにより、コンパイル時間の短縮と保守性の向上が図られています。